# Base URL of the backend API. Defaults to http://localhost:8000 if
# unset — only needed here for pointing the built frontend at a
# deployed backend, or a backend running on a non-default port.
VITE_API_BASE_URL=http://localhost:8000
